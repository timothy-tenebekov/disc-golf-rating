html
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/styles.css")
        block head
    body
        div.container.container-vert
            div.panel.panel-menu
                block panel-menu
            div.panel.panel-main
                block panel-main
        script.
            function withSign(result) {
                return (result > 0 ? '+' : '') + result;
            }
        block script
